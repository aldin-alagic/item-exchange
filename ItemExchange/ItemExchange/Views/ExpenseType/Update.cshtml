
@model ItemExchange.Models.ExpenseType

@{
    ViewData["Title"] = "Update expense type";
}

<form class="d-flex justify-content-center mt-5" method="post">
    <div class="card col-6 px-5 py-4">
        <div>
            <h2 class="text-black-50">Update expense type</h2>
            <hr class="w-100"/>
        </div>
        <div class="row">
            <div class="col-12">
                <input asp-for="Id" hidden />
                <div class="form-group">
                    <div>
                        <label class="form-label" asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row">
                    <input type="submit" class="btn btn-primary mx-3" value="Update" />
                    <a asp-action="Index" class="btn btn-danger">Back</a>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{ <partial name="_ValidationScriptsPartial" /> }
}